1,登录：
	mysql -uroot -p123
2,创建数据库：
	create database 库名;
	create database ;
3,创建表
	create table 表名(
		列名1 类型1,
		列名2 类型2,
		列名3 类型3
	);
	create table hostel(id int, name varchar(50), age int(15), hobby varchar(50))
4,添加数据
	insert into 表名 values(
		第一列的值,
		第二列的值,
		第三列的值
	);
	insert into hostel values(1, 'ddw', 22, 'study');
	insert into hostel values(2, 'yqs', 21, 'phone');
	insert into hostel values(3, 'sss', 23, 'dnf');
5,查找数据
	select * from 表名;
	    *表示的是要把所有的列都查找出来
	select * from hostel;
6,更新数据
	update 表名 set 列1=值1;
	    把列1中所有的值都设置为值1
	update hostel set id=2;
7,删除数据
	delete 表名;
	    把表中的数据全删了。
    delete hostel;
8,删除表
	drop table 表名;
	drop table hostel;
9,删除库
	drop database 库名;
    drop database hostel;
10,查找表的基本结构
	desc 表名;
    desc hostel;
11,进入某个数据库
	use 库名;
	use database;
12,查看某个数据库中所有的表
	show tables;
13,查看mysql中所有的库
	show databases;
14,退出
	quit;
15如果语句无法结束：
	打个分号后回车
  如果分号也不管用：
	看看是不是少了一个单引号
	所以打个：';
	回车
16,遇到带near的错误的时候：
	就是语句打错了，找不到错误就从打


